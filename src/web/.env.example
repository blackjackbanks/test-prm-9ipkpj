# API Configuration
# Base URL for backend API endpoints (use HTTPS in production)
VITE_API_BASE_URL=http://localhost:8000

# WebSocket Configuration
# WebSocket server URL for real-time features (use WSS in production)
VITE_WS_URL=ws://localhost:8000/ws

# Environment Configuration
# Application environment (development, staging, production)
VITE_APP_ENV=development

# Auth0 Configuration
# Auth0 tenant domain for primary authentication
VITE_AUTH0_DOMAIN=your-auth0-domain.auth0.com
# Auth0 application client ID
VITE_AUTH0_CLIENT_ID=your-auth0-client-id

# SSO Provider Configuration
# Google OAuth client ID for SSO integration
VITE_GOOGLE_CLIENT_ID=your-google-client-id
# Microsoft OAuth client ID for SSO integration
VITE_MICROSOFT_CLIENT_ID=your-microsoft-client-id
# Apple OAuth client ID for SSO integration
VITE_APPLE_CLIENT_ID=your-apple-client-id

# API Version Configuration
# API version identifier for versioned endpoints
VITE_API_VERSION=v1

# Application Configuration
# Application name for display and configuration
VITE_APP_NAME=COREos

# SECURITY NOTICE:
# 1. Never commit the actual .env file to version control
# 2. Use secure secret management in production
# 3. Rotate credentials regularly
# 4. Implement proper access controls
# 5. Use HTTPS/WSS protocols in production
# 6. Validate all variables before deployment